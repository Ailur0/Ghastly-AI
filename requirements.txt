# Ghost Interview Agent — Dependencies
# Install: pip install -r requirements.txt

# Audio capture
sounddevice==0.5.5
soundcard==0.4.6
numpy==2.5.1

# Ghost overlay (PyQt5 — primary)
PyQt5==5.15.11

# HTTP client (Groq + Ollama cloud APIs)
requests==2.34.2

# That's it. No torch, no openai-whisper, no heavy downloads.
# STT: Groq cloud API (whisper-large-v3)
# LLM: Ollama cloud API (glm-4.5)